// Area total cilindro
// At = 2*pi*R*(R + H)

// const pi = Math.PI;
const raioBase = 3;
const altura = 1;

const areaTotal = 2 * raioBase * (raioBase + altura);

console.log(`Area total do cilindro = ${areaTotal}pi`);